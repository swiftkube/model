//
// Copyright 2020-2026 Swiftkube Project
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

//
// Generated by Swiftkube:ModelGen
// Kubernetes v1.34.6
// resource.v1beta2.DeviceRequestAllocationResult
//

import Foundation

// MARK: - resource.v1beta2.DeviceRequestAllocationResult

public extension resource.v1beta2 {

	///
	/// DeviceRequestAllocationResult contains the allocation result for one request.
	///
	struct DeviceRequestAllocationResult: KubernetesResource {
		///
		/// AdminAccess indicates that this device was allocated for administrative access. See the corresponding request field for a definition of mode.
		///
		/// This is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled.
		///
		public var adminAccess: Bool?
		///
		/// BindingConditions contains a copy of the BindingConditions from the corresponding ResourceSlice at the time of allocation.
		///
		/// This is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates.
		///
		public var bindingConditions: [String]?
		///
		/// BindingFailureConditions contains a copy of the BindingFailureConditions from the corresponding ResourceSlice at the time of allocation.
		///
		/// This is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates.
		///
		public var bindingFailureConditions: [String]?
		///
		/// ConsumedCapacity tracks the amount of capacity consumed per device as part of the claim request. The consumed amount may differ from the requested amount: it is rounded up to the nearest valid value based on the device’s requestPolicy if applicable (i.e., may not be less than the requested amount).
		///
		/// The total consumed capacity for each device must not exceed the DeviceCapacity's Value.
		///
		/// This field is populated only for devices that allow multiple allocations. All capacity entries are included, even if the consumed amount is zero.
		///
		public var consumedCapacity: [String: Quantity]?
		///
		/// Device references one device instance via its name in the driver's resource pool. It must be a DNS label.
		///
		public var device: String
		///
		/// Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.
		///
		/// Must be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver.
		///
		public var driver: String
		///
		/// This name together with the driver name and the device name field identify which device was allocated (`<driver name>/<pool name>/<device name>`).
		///
		/// Must not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes.
		///
		public var pool: String
		///
		/// Request is the name of the request in the claim which caused this device to be allocated. If it references a subrequest in the firstAvailable list on a DeviceRequest, this field must include both the name of the main request and the subrequest using the format <main request>/<subrequest>.
		///
		/// Multiple devices may have been allocated per request.
		///
		public var request: String
		///
		/// ShareID uniquely identifies an individual allocation share of the device, used when the device supports multiple simultaneous allocations. It serves as an additional map key to differentiate concurrent shares of the same device.
		///
		public var shareID: String?
		///
		/// A copy of all tolerations specified in the request at the time when the device got allocated.
		///
		/// The maximum number of tolerations is 16.
		///
		/// This is an alpha field and requires enabling the DRADeviceTaints feature gate.
		///
		public var tolerations: [resource.v1beta2.DeviceToleration]?
		///
		/// Default memberwise initializer
		///
		public init(
			adminAccess: Bool? = nil,
			bindingConditions: [String]? = nil,
			bindingFailureConditions: [String]? = nil,
			consumedCapacity: [String: Quantity]? = nil,
			device: String,
			driver: String,
			pool: String,
			request: String,
			shareID: String? = nil,
			tolerations: [resource.v1beta2.DeviceToleration]? = nil
		) {
			self.adminAccess = adminAccess
			self.bindingConditions = bindingConditions
			self.bindingFailureConditions = bindingFailureConditions
			self.consumedCapacity = consumedCapacity
			self.device = device
			self.driver = driver
			self.pool = pool
			self.request = request
			self.shareID = shareID
			self.tolerations = tolerations
		}
	}
}

///
/// Codable conformance
///
public extension resource.v1beta2.DeviceRequestAllocationResult {

	private enum CodingKeys: String, CodingKey {

		case adminAccess = "adminAccess"
		case bindingConditions = "bindingConditions"
		case bindingFailureConditions = "bindingFailureConditions"
		case consumedCapacity = "consumedCapacity"
		case device = "device"
		case driver = "driver"
		case pool = "pool"
		case request = "request"
		case shareID = "shareID"
		case tolerations = "tolerations"
	}

	init(from decoder: Decoder) throws {
		let container = try decoder.container(keyedBy: CodingKeys.self)
		self.adminAccess = try container.decodeIfPresent(Bool.self, forKey: .adminAccess)
		self.bindingConditions = try container.decodeIfPresent([String].self, forKey: .bindingConditions)
		self.bindingFailureConditions = try container.decodeIfPresent([String].self, forKey: .bindingFailureConditions)
		self.consumedCapacity = try container.decodeIfPresent([String: Quantity].self, forKey: .consumedCapacity)
		self.device = try container.decode(String.self, forKey: .device)
		self.driver = try container.decode(String.self, forKey: .driver)
		self.pool = try container.decode(String.self, forKey: .pool)
		self.request = try container.decode(String.self, forKey: .request)
		self.shareID = try container.decodeIfPresent(String.self, forKey: .shareID)
		self.tolerations = try container.decodeIfPresent([resource.v1beta2.DeviceToleration].self, forKey: .tolerations)
	}

	func encode(to encoder: Encoder) throws {
		var encodingContainer = encoder.container(keyedBy: CodingKeys.self)

		try encodingContainer.encode(adminAccess, forKey: .adminAccess)
		try encodingContainer.encode(bindingConditions, forKey: .bindingConditions)
		try encodingContainer.encode(bindingFailureConditions, forKey: .bindingFailureConditions)
		try encodingContainer.encode(consumedCapacity, forKey: .consumedCapacity)
		try encodingContainer.encode(device, forKey: .device)
		try encodingContainer.encode(driver, forKey: .driver)
		try encodingContainer.encode(pool, forKey: .pool)
		try encodingContainer.encode(request, forKey: .request)
		try encodingContainer.encode(shareID, forKey: .shareID)
		try encodingContainer.encode(tolerations, forKey: .tolerations)
	}
}
