//
// Copyright 2020-2026 Swiftkube Project
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

//
// Generated by Swiftkube:ModelGen
// Kubernetes v1.34.6
// core.v1.PodCertificateProjection
//

import Foundation

// MARK: - core.v1.PodCertificateProjection

public extension core.v1 {

	///
	/// PodCertificateProjection provides a private key and X.509 certificate in the pod filesystem.
	///
	struct PodCertificateProjection: KubernetesResource {
		///
		/// Write the certificate chain at this path in the projected volume.
		///
		/// Most applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation.
		///
		public var certificateChainPath: String?
		///
		/// Write the credential bundle at this path in the projected volume.
		///
		/// The credential bundle is a single file that contains multiple PEM blocks. The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private key.
		///
		/// The remaining blocks are CERTIFICATE blocks, containing the issued certificate chain from the signer (leaf and any intermediates).
		///
		/// Using credentialBundlePath lets your Pod's application code make a single atomic read that retrieves a consistent key and certificate chain.  If you project them to separate files, your application code will need to additionally check that the leaf certificate was issued to the key.
		///
		public var credentialBundlePath: String?
		///
		/// Write the key at this path in the projected volume.
		///
		/// Most applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation.
		///
		public var keyPath: String?
		///
		/// The type of keypair Kubelet will generate for the pod.
		///
		/// Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384", "ECDSAP521", and "ED25519".
		///
		public var keyType: String
		///
		/// maxExpirationSeconds is the maximum lifetime permitted for the certificate.
		///
		/// Kubelet copies this value verbatim into the PodCertificateRequests it generates for this projection.
		///
		/// If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver will reject values shorter than 3600 (1 hour).  The maximum allowable value is 7862400 (91 days).
		///
		/// The signer implementation is then free to issue a certificate with any lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600 seconds (1 hour).  This constraint is enforced by kube-apiserver. `kubernetes.io` signers will never issue certificates with a lifetime longer than 24 hours.
		///
		public var maxExpirationSeconds: Int32?
		///
		/// Kubelet's generated CSRs will be addressed to this signer.
		///
		public var signerName: String
		///
		/// Default memberwise initializer
		///
		public init(
			certificateChainPath: String? = nil,
			credentialBundlePath: String? = nil,
			keyPath: String? = nil,
			keyType: String,
			maxExpirationSeconds: Int32? = nil,
			signerName: String
		) {
			self.certificateChainPath = certificateChainPath
			self.credentialBundlePath = credentialBundlePath
			self.keyPath = keyPath
			self.keyType = keyType
			self.maxExpirationSeconds = maxExpirationSeconds
			self.signerName = signerName
		}
	}
}

///
/// Codable conformance
///
public extension core.v1.PodCertificateProjection {

	private enum CodingKeys: String, CodingKey {

		case certificateChainPath = "certificateChainPath"
		case credentialBundlePath = "credentialBundlePath"
		case keyPath = "keyPath"
		case keyType = "keyType"
		case maxExpirationSeconds = "maxExpirationSeconds"
		case signerName = "signerName"
	}

	init(from decoder: Decoder) throws {
		let container = try decoder.container(keyedBy: CodingKeys.self)
		self.certificateChainPath = try container.decodeIfPresent(String.self, forKey: .certificateChainPath)
		self.credentialBundlePath = try container.decodeIfPresent(String.self, forKey: .credentialBundlePath)
		self.keyPath = try container.decodeIfPresent(String.self, forKey: .keyPath)
		self.keyType = try container.decode(String.self, forKey: .keyType)
		self.maxExpirationSeconds = try container.decodeIfPresent(Int32.self, forKey: .maxExpirationSeconds)
		self.signerName = try container.decode(String.self, forKey: .signerName)
	}

	func encode(to encoder: Encoder) throws {
		var encodingContainer = encoder.container(keyedBy: CodingKeys.self)

		try encodingContainer.encode(certificateChainPath, forKey: .certificateChainPath)
		try encodingContainer.encode(credentialBundlePath, forKey: .credentialBundlePath)
		try encodingContainer.encode(keyPath, forKey: .keyPath)
		try encodingContainer.encode(keyType, forKey: .keyType)
		try encodingContainer.encode(maxExpirationSeconds, forKey: .maxExpirationSeconds)
		try encodingContainer.encode(signerName, forKey: .signerName)
	}
}
