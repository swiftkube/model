//
// Copyright 2020 Swiftkube Project
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//

///
/// Generated by Swiftkube:ModelGen
/// Kubernetes v1.29.6
/// core.v1.PodAffinityTerm
///

import Foundation

// MARK: - core.v1.PodAffinityTerm

public extension core.v1 {

	///
	/// Defines a set of pods (namely those matching the labelSelector relative to the given namespace(s)) that this pod should be co-located (affinity) or not co-located (anti-affinity) with, where co-located is defined as running on a node whose value of the label with key <topologyKey> matches that of any node on which a pod of the set of pods is running
	///
	struct PodAffinityTerm: KubernetesResource {
		///
		/// A label query over a set of resources, in this case pods. If it's null, this PodAffinityTerm matches with no Pods.
		///
		public var labelSelector: meta.v1.LabelSelector?
		///
		/// MatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `LabelSelector` as `key in (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector. Also, MatchLabelKeys cannot be set when LabelSelector isn't set. This is an alpha field and requires enabling MatchLabelKeysInPodAffinity feature gate.
		///
		public var matchLabelKeys: [String]?
		///
		/// MismatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `LabelSelector` as `key notin (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both MismatchLabelKeys and LabelSelector. Also, MismatchLabelKeys cannot be set when LabelSelector isn't set. This is an alpha field and requires enabling MatchLabelKeysInPodAffinity feature gate.
		///
		public var mismatchLabelKeys: [String]?
		///
		/// A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod's namespace". An empty selector ({}) matches all namespaces.
		///
		public var namespaceSelector: meta.v1.LabelSelector?
		///
		/// namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod's namespace".
		///
		public var namespaces: [String]?
		///
		/// This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed.
		///
		public var topologyKey: String
		///
		/// Default memberwise initializer
		///
		public init(
			labelSelector: meta.v1.LabelSelector? = nil,
			matchLabelKeys: [String]? = nil,
			mismatchLabelKeys: [String]? = nil,
			namespaceSelector: meta.v1.LabelSelector? = nil,
			namespaces: [String]? = nil,
			topologyKey: String
		) {
			self.labelSelector = labelSelector
			self.matchLabelKeys = matchLabelKeys
			self.mismatchLabelKeys = mismatchLabelKeys
			self.namespaceSelector = namespaceSelector
			self.namespaces = namespaces
			self.topologyKey = topologyKey
		}
	}
}

///
/// Codable conformance
///
public extension core.v1.PodAffinityTerm {

	private enum CodingKeys: String, CodingKey {

		case labelSelector = "labelSelector"
		case matchLabelKeys = "matchLabelKeys"
		case mismatchLabelKeys = "mismatchLabelKeys"
		case namespaceSelector = "namespaceSelector"
		case namespaces = "namespaces"
		case topologyKey = "topologyKey"
	}

	init(from decoder: Decoder) throws {
		let container = try decoder.container(keyedBy: CodingKeys.self)
		self.labelSelector = try container.decodeIfPresent(meta.v1.LabelSelector.self, forKey: .labelSelector)
		self.matchLabelKeys = try container.decodeIfPresent([String].self, forKey: .matchLabelKeys)
		self.mismatchLabelKeys = try container.decodeIfPresent([String].self, forKey: .mismatchLabelKeys)
		self.namespaceSelector = try container.decodeIfPresent(meta.v1.LabelSelector.self, forKey: .namespaceSelector)
		self.namespaces = try container.decodeIfPresent([String].self, forKey: .namespaces)
		self.topologyKey = try container.decode(String.self, forKey: .topologyKey)
	}

	func encode(to encoder: Encoder) throws {
		var encodingContainer = encoder.container(keyedBy: CodingKeys.self)

		try encodingContainer.encode(labelSelector, forKey: .labelSelector)
		try encodingContainer.encode(matchLabelKeys, forKey: .matchLabelKeys)
		try encodingContainer.encode(mismatchLabelKeys, forKey: .mismatchLabelKeys)
		try encodingContainer.encode(namespaceSelector, forKey: .namespaceSelector)
		try encodingContainer.encode(namespaces, forKey: .namespaces)
		try encodingContainer.encode(topologyKey, forKey: .topologyKey)
	}
}
